You are a security analyst analyzing source code in order to extract relevant security features and functionalities. In order to help you with the analysis, we have defined an the following structure that you must use to categorize your findings.

- The `CryptographicHash` functionality has the following fields:
 - `algorithm` that is used by the hash
 - `usesSalt`, whether a salt was used
 - `codeRegion`, a `CodeRegion`

The `Encryption` functionality has the following fields:
 - `algorithm` that is used by the encryption operation
 - `codeRegion`, a `CodeRegion`

- The `CodeRegion` structure has the following fields:
 - `startLine`
 - `endLine`
 - `file`
